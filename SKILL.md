---
name: personal-knowledge-base
version: 1.0.0
description: "个人知识库：把用户自己产生的信息（会议、笔记、文档、任务）变成可以被推理的原材料。当用户说：帮我写周报、分析我上周做了什么、下周应该做什么、基于我的信息研究某个话题、整合我的信息——立即使用这个 skill。也适用于：我想回顾、我的记录在哪、帮我梳理、我最近在忙什么。"
---

# 个人知识库 (Personal Knowledge Base)

## 本机私有配置

这个 skill 可以打包分发，包内不得保存任何用户密钥、Token、Cookie、App Secret 或个人 Obsidian 路径。

每次需要访问平台或写入 Obsidian 前，先读取本机私有配置，优先级如下：

1. 环境变量：
   - `PKB_OBSIDIAN_VAULT`
   - `GET_NOTES_API_KEY`
   - `IMA_API_KEY`
   - 其他平台按对应 setup 文档约定的环境变量读取
2. 本机私有文件：
   - macOS/Linux: `$HOME/.codex/pkb.local.json`
   - Windows: `%USERPROFILE%\.codex\pkb.local.json`

`pkb.local.json` 可以保存非分发的本机配置，例如：

```json
{
  "obsidianVault": "/Users/you/Documents/Company/Company",
  "platforms": ["get-notes"]
}
```

规则：
- 不要把 `pkb.local.json` 放入 skill 包或提交到仓库。
- 不要在 `SKILL.md` 或 `references/` 中写入真实密钥。
- 不要在对话中回显完整密钥；如果必须确认，只显示前后各 3-4 位。
- 如果本机私有配置文件不存在，自动创建一个只包含非敏感默认值的文件，并明确告知用户已创建以及文件路径。
- 当用户要求把个人知识库、Get笔记、会议记录或其他 PKB 内容同步到 Obsidian 时，必须写入私有配置指定的 vault；不要通过扫描 `.obsidian` 或使用最近打开的 vault 来猜测目标位置。
- 写入前先验证 `<vault>\.obsidian` 存在；如果不存在，停止并向用户确认，不要改写到其他 vault。

## Obsidian 同步与双链策略

当任务涉及把 Get笔记、会议记录、转录、个人笔记或知识库内容写入 Obsidian 时，必须先读取并遵循 `references/obsidian-linking-strategy.md`。

当用户主动收录一条商业相关材料，或要求重构个人知识库时，必须同时读取并遵循 `references/business-judgment-analysis.md`。该知识库的核心不是保存资料，而是把用户主动收录的材料转化为商业判断力、关系判断、机会识别和可迁移方法。

核心要求：
- 不要只复制粘贴原始内容；要先判断内容应如何进入主笔记、Transcript 证据和 Map 综合页。Source 不再默认生成，除非它保存了主笔记和 Transcript 都没有覆盖的原始材料。
- 主笔记结构必须采用“材料类型基础骨架 + 工具槽位”的半结构化方式：先匹配客户/合作会议、政策/培训/指南、产品/交付/工作流、商业机会/行业材料、个人反思等基础骨架，再按证据填入 `镜头`、`以案释法`、`深水区`、`QA 边界`、`原始内容主线`、`可沉淀工具` 等工具槽位。不要写成纯自由结构，也不要写成死模板。
- 对政策讲解、培训、指南、专家讲解、产品 walkthrough 等“原始内容讲解型材料”，必须优先保留原始内容主线、规则/标准/条件、QA 边界、例外、红线和可执行清单，然后再做商业判断与项目启发。
- 写入 Obsidian 的主笔记必须使用统一的 Obsidian 属性/frontmatter 集中呈现时间、来源、类型、知识库、标签、原文/转录链接等字段；正文里不要再重复一个“基本信息”表格。
- 正文不要添加与文件名重复的 H1 标题；文件名应承担页面标题，并保留日期前缀。
- 主笔记必须像一篇可长期阅读的知识沉淀，而不是生成过程记录；正文不得出现“旧稿”“本次重写”“样张”“批量跑”“我这次”等生产过程痕迹，也不要用对比旧版本的方式表达判断。
- 主笔记不应默认复述 Get笔记摘要；应先判断这条材料为什么值得收录，再提炼商业上可迁移的价值。
- 主库目录按未来使用场景组织，不按来源渠道组织；`Get笔记`、飞书、IMA 等只作为属性里的来源平台。主笔记应放入“商业判断/机会判断、交易与定价、客户与关系、产品与交付、组织与产业、融资与资本”等宽入口，或放入相应的个人判断/项目目录。
- 一篇长笔记只能选择一个主用途目录；其他语义维度用 `主用途`、`适用问题`、`相关项目`、`相关主题`、双链和 Map 表达，不复制多份文件。
- 不要自动把所有名词都变成双链；只链接稳定对象、长期主题、项目、组织、人物和可复用概念。
- 会议/转录类材料优先作为证据来源；逐字稿和 Source 应放在 `_Evidence/<来源平台>/...` 这类档案层，平时不作为主库浏览入口。真正的知识沉淀放到主笔记、主题页、项目页或 MOC/Hub 中。
- 如果 API 能拿到完整逐字稿，逐字稿应独立保存并从主笔记链接过去；不要把长逐字稿直接塞进主笔记。

## 核心原则

这个 skill 只处理**用户自己产生的信息**：
- ✅ 你参加的会议、你写的笔记、你的任务、你的日程、你的文档
- ❌ 别人写的文章、行业新闻、公开内容（这些是参考资料，不是你的知识库）

---

## 第一步：检查配置

**每次触发时，先检查本机私有配置：**

只有当任务需要读取或写入飞书/Lark 内容时，才调用 `lark-cli`。凡是涉及飞书数据访问，必须使用 `lark-cli`；如果当前设备没有 `lark-cli`，先安装或更新 `@larksuite/cli@latest`，不要改用其他飞书访问方式替代。

1. 读取环境变量和本机私有配置文件（macOS/Linux 为 `$HOME/.codex/pkb.local.json`，Windows 为 `%USERPROFILE%\.codex\pkb.local.json`）
2. 检查已启用平台和必要凭据是否存在
3. 检查 Obsidian vault 是否存在

- 如果本机私有配置文件 **不存在**：
  1. 自动创建目录 `$HOME/.codex` 或 `%USERPROFILE%\.codex`（如果目录也不存在）
  2. 自动创建 `pkb.local.json`
  3. 写入非敏感默认配置：

```json
{
  "platforms": [],
  "sync": {
    "defaultRange": "yesterday"
  },
  "createdAt": "<当前 ISO 时间>"
}
```

  4. 明确告诉用户：已创建本机私有配置文件、文件路径是什么、还需要补充哪些非敏感配置（例如 Obsidian vault 路径或启用平台）

- 如果配置 **不存在或不完整** → 走【首次配置流程】
- 如果配置 **存在** → 直接走【任务选择】

---

## 首次配置流程

**第一句话说：**

> 在开始之前，我需要了解你在用哪些平台。请选择你实际在用的（可多选）：
>
> A. 飞书（云文档 / 多维表格 / 会议 / 妙记 / 日历）
> B. 腾讯会议
> C. Get笔记
> D. IMA 知识库（腾讯）
> E. 以上都没有 / 其他平台

等用户回答后，根据选择，**逐个引导配置**。每个平台的详细配置步骤见 `references/` 目录：

| 平台 | 配置文件 |
|------|----------|
| 飞书 | `references/setup-lark.md` |
| 腾讯会议 | `references/setup-tencent-meeting.md` |
| Get笔记 | `references/setup-get-notes.md` |
| IMA | `references/setup-ima.md` |

配置完成后，将非敏感配置写入本机私有配置文件。密钥优先写入环境变量；如果用户明确要求写入本机私有文件，也只能写入 `pkb.local.json`，并提醒该文件不要分享。

```json
{
  "platforms": ["lark", "get-notes"],
  "weeklyReportSections": null,
  "configuredAt": "2026-05-26T00:00:00.000Z"
}
```

**告诉用户：** 配置信息已保存，下次使用无需重新配置。如需修改，说"重置知识库配置"即可。

---

## 任务选择

配置完成后（或已有配置时），**问用户：**

> 你想做什么？
>
> 1. 生成周报
> 2. 下周行动规划（基于上周的真实记录）
> 3. 跨平台研究某个话题

等用户选择。

---

## 模式一：生成周报

### 步骤

**1. 确认时间范围**

> 生成哪一周的周报？（默认：上周一到上周日）

**2. 从各平台拉取原材料**

根据用户已配置的平台，分别获取：

| 平台 | 获取内容 | 工具 |
|------|----------|------|
| 飞书视频会议 / 妙记 | 会议列表、纪要、逐字稿 | lark-cli |
| 腾讯会议 | 会议主题、时间、时长 | tencent-meeting MCP |
| 飞书多维表格 | 已完成/进行中的任务 | lark-cli |
| 飞书云文档 | 本周创建/编辑的文档 | lark-cli |
| 飞书日历 | 本周日程安排 | lark-cli |
| Get笔记 | 本周的音视频转录记录 | Get笔记 API |

**3. 检查是否有已保存的周报结构**

从本机私有配置文件读取 `weeklyReportSections`。

- 如果有 → 按已有结构生成
- 如果没有 → 使用默认结构（见下方）

**4. 生成周报**

**默认周报结构：**

```
【本周周报】YYYY年第N周（MM月DD日 - MM月DD日）

▌ 本周完成的事
（来源：会议记录 + 任务 + 文档）
- 每条都注明来源，例如："完成X方案 [飞书文档·5月12日]"

▌ 本周参与的会议
（来源：飞书妙记 / 腾讯会议）
- 日期、主题、关键结论（非流水账）

▌ 本周的关键决策
（从会议纪要和文档中提取有决策意义的内容）

▌ 尚未完成 / 待跟进
（来源：任务列表 + 会议中提到的待办）

▌ 下周已有安排
（来源：飞书日历）
```

**5. 询问是否保存结构偏好**

> 这个结构你满意吗？如果需要调整（增减板块、改变顺序），告诉我，我会记住你的偏好，下次直接用。

用户确认后，将结构存入 storage。

---

## 模式二：下周行动规划

**核心承诺：每个行动建议必须附带原始证据，不靠推断，不靠感觉。**

### 步骤

**1. 拉取上周所有原材料**（同周报的数据获取步骤）

**2. 提取四类信息**

从原材料中逐条提取：

| 类型 | 判断标准 | 来源 |
|------|----------|------|
| 已完成的事 | 明确做了、交付了、结束了 | 任务/文档/会议 |
| 作出的决定 | 会议结论、文档定稿、明确承诺 | 妙记/纪要 |
| 悬而未决的事 | 提到了但没下文、推迟的、待确认的 | 会议/任务 |
| 被忽略的信号 | 提到了但没有对应行动的问题/机会 | 笔记/会议 |

**3. 生成行动建议**

格式必须是：

```
【下周行动规划】

行动 1：[具体行动]
为什么：[原因，用一句话]
证据：[上周第X天，在Y会议/文档中，你说了/出现了Z]

行动 2：...

---
【待观察，暂不行动】
- [事项]：[等待的条件是什么]

【可以放下的事】
- [事项]：[为什么可以放下]
```

**4. 透明声明**

在输出开头说明：
> 以下内容基于你上周在 [平台列表] 的真实记录，所有建议均可追溯到具体来源。

---

## 模式三：跨平台话题研究

**适用于：** 想深入了解自己过去在某个话题上积累了什么。

### 步骤

**1. 明确话题**

> 你想研究的话题是什么？请用几个关键词描述。

**2. 全平台检索**

在所有已配置平台中，搜索与话题相关的内容：

```
搜索范围：
- 飞书文档：标题 + 全文搜索
- 飞书多维表格：字段搜索
- 飞书妙记：会议主题 + 纪要内容
- Get笔记：转录内容关键词
- IMA：知识库节点搜索
```

**3. 整合输出**

```
【话题研究：XXX】

你在这个话题上积累的内容：

▌ 文档和笔记（N条）
- [标题] [平台] [日期] [摘要一句话]

▌ 相关会议（N场）
- [日期] [主题] [关键结论]

▌ 你自己的判断/观点
（从文档和会议记录中提取你表达的明确立场）

▌ 尚未解答的问题
（你提过但没有答案的问题）
```

---

## 重置配置

如果用户说"重置知识库配置"或"重新配置"：

删除或重建本机私有配置文件中的非敏感配置。环境变量中的密钥不自动删除，除非用户明确要求。

然后重新走首次配置流程。

---

## 重要提醒

**每次操作完成后，告诉用户：**
1. 我从哪些平台获取了数据
2. 时间范围是什么
3. 如果某个平台获取失败，明确说明（不要静默跳过）

**不要做的事：**
- 不要把任何"可能"、"也许"的内容当作事实输出
- 不要在没有来源的情况下生成内容（宁可告诉用户"这个平台暂时没有数据"）
- 不要主动推断用户的意图，不明确的地方就问
